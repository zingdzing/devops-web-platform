{{- if .Values.monitoring.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "devops-web-platform.fullname" . }}-grafana-dashboard
  labels:
    {{- include "devops-web-platform.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  devops-web-platform-overview.json: |-
{{ .Files.Get "files/devops-web-platform-overview.json" | indent 4 }}
{{- end }}
